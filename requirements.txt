# Dependencies are declared in pyproject.toml (single source of truth).
# Installing this file installs the package in editable mode with all deps:
#   pip install -r requirements.txt
# Optional extras:
#   pip install -e ".[dev]"    # test/lint tooling
#   pip install -e ".[groq]"   # enable the Groq LLM provider
-e .
